package dsa.DfsBfsAMatrixM;

//The following lines aren't necessary for the matrix. They're written only to add headers (names) for the matrix by added nodes (names).
public class NodeM {
    
    char data;
    
    public NodeM(char data) {
        this.data = data;
    }
    
}
